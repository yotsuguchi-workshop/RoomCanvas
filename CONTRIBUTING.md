# 改善への参加

Issue・Pull Requestを利用してください。非商用向けソース公開であり、投稿する変更にもこのリポジトリのライセンスを適用できる権利が必要です。他人のコード・画像を無断で持ち込まないでください。

## 開発

1. fork / cloneし、自分の作業ブランチを作る。
2. Xcodeでビルド。iPadOS 15互換を保つ。
3. `scripts/check-policies.command`を実行。
4. 画面変更は横向き4:3で確認する。
5. BLE変更は実機で重複、無操作、休止・再開、連打抑止を確認する。

PRには変更理由と確認結果を記載してください。Webhookの秘密情報、署名ファイル、実機識別子、個人予定を載せないでください。

チェックはmacOS上で共通ロジックを抽出して検証します。UI・Core Bluetoothの無線挙動やWebhook実配信を代替するものではありません。
