name: Check
on: [push, pull_request]
permissions:
  contents: read
jobs:
  policy:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./scripts/check-policies.command
